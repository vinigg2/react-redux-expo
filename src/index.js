import React, { Component } from 'react';
import Router from './helpers/router';


export default class AppSource extends React.Component {
    render() {
        return (
            <Router />
        )
    }
}